// SPDX-License-Identifier: BUSL-1.1
pragma solidity ^0.8.13;

contract IsPrime {
    /**
     * The goal of this exercise is to return if "number" is prime or not (true or false)
    */

    // Prime number is a number that can only be multiplied by itself and 1. (7 => 7 * 1);
    // Non prime number is a number that can be multiplied by more than 1 and it self (8 => 8 * 1; 4 * 2);
    function isPrime(uint256 number) public view returns (bool) {
        // your code here
    }
}
